import numpy as np\nimport pandas as pd\nimport seaborn as sns\nimport matplotlib.pyplot as plt\n\n# List of Bank Nifty 14 constituent stocks\nstocks = [\n    'RELIANCE',\n    'HDFC_BANK',\n    'INFOSYS',\n    'ICICI_BANK',\n    'KOTAK_BANK',\n    'HDFC',\n    'ITC',\n    'TCS',\n    'BPCL',\n    'ONGC',\n    'LT',\n    'TITAN',\n    'HINDUNILVR',\n    'AXIS_BANK'\n]  \n\n# Function to generate heatmap for correlations\ndef generate_heatmap(data):\n    correlation = data.corr()\n    plt.figure(figsize=(10, 8))\n    sns.heatmap(correlation, annot=True, cmap='coolwarm', fmt='.2f')\n    plt.title('Price Correlation Heatmap for Bank Nifty 14 Stocks')\n    plt.show()\n\n# Example of how to use the generate_heatmap function\n\n# Assuming you have a DataFrame 'price_data' with the stock prices\n# price_data = pd.DataFrame(...)\n# generate_heatmap(price_data)\n